---
import BaseLayout from '../../layouts/BaseLayout.astro';
import HeroInteractivo from '../../components/HeroInteractivo.astro';
import CardServicio from '../../components/CardServicio.astro';
import { getTranslation } from '../../utils/i18n';

const lang = 'en';
const t = (key: string) => getTranslation(lang, key);
---

<BaseLayout title="SPS - Home" lang={lang}>
  <HeroInteractivo 
    title={t('hero.title')}
    subtitle={t('hero.subtitle')}
    ctaText={t('hero.cta')}
    ctaLink={`/${lang}/${t('nav.services_url')}`}
  />

  <section class="services-preview">
    <h2>Our Featured Services</h2>
    <div class="grid">
      <CardServicio 
        title="Web Development" 
        description="Modern, fast, and scalable websites using the best technologies."
        icon="💻"
      />
      <CardServicio 
        title="UI/UX Design" 
        description="Intuitive user interfaces focused on the best experience."
        icon="🎨"
      />
    </div>
  </section>
</BaseLayout>

<style>
  .services-preview {
    max-width: 1200px;
    margin: 4rem auto;
    padding: 0 2rem;
  }
  h2 {
    text-align: center;
    margin-bottom: 3rem;
    color: var(--color-primary);
  }
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }
</style>
